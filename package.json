{
  "name": "trajectry",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "test": "tsx src/game/sanity.ts",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host",
    "cap:add:ios": "cap add ios",
    "cap:add:android": "cap add android",
    "cap:sync": "npm run build && cap sync"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2",
    "vite": "^8.2.2"
  },
  "dependencies": {
    "@capacitor/core": "^8.5.0"
  }
}
